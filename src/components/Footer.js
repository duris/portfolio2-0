import Form from "./Form";

export default function Footer(params) {
  return(
    <footer className='app-footer'>             
        <span>&copy; duris.dev</span>
        <Form/>
      </footer>
  )
}